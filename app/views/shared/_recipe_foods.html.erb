<tr>
    <td><%= food.name %></td>
    <td><%= food.quantity %></td>
    <td><%= food.price * food.quantity %></td>
    <td>
      <%= link_to 'Modify', recipe_foods_path(food.id), class: "text-blue-600 underline", data: { "turbo-method": :edit} %>
    </td>
    <td>
      <%= link_to 'Remove', recipe_foods_path(food.id), class: "text-blue-600 underline", data: { "turbo-method": :delete} %>
    </td>
</tr>
  